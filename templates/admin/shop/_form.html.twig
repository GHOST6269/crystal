{{ form_start(form) }}
  <div class="form-grid">
    {{ form_row(form.name) }}
    {{ form_row(form.address) }}
    {{ form_row(form.hours) }}
    {{ form_row(form.focus) }}
    {{ form_row(form.position) }}
  </div>
  <div style="margin-top: 20px; display: flex; gap: 12px;">
    <button class="btn" type="submit">Enregistrer</button>
    <a class="btn secondary" href="{{ path('admin_shop_index') }}">Retour</a>
  </div>
{{ form_end(form) }}
