{{ form_start(form) }}
  <div class="form-grid">
    {{ form_row(form.label) }}
    {{ form_row(form.price) }}
    {{ form_row(form.position) }}
  </div>
  <div style="margin-top: 20px; display: flex; gap: 12px;">
    <button class="btn" type="submit">Enregistrer</button>
    <a class="btn secondary" href="{{ path('admin_tariff_index') }}">Retour</a>
  </div>
{{ form_end(form) }}
